# Supervisor

### Author: White

Containers are boring, so I made this clearly well designed supervisor to run untrusted code. What could possibly go wrong?

Infra Note: This chall has to be instanced. The players will get code execution inside the container. Using red pwn jail is not possible because suid is required to make the chall work. Also it needs SYS_PTRACE.
