services:
  chall:
    build: .
    container_name: chall

    privileged: true

    ports:
      - "5000:5000"

    restart: unless-stopped

    read_only: true

    tmpfs:
      - /tmp:size=16M
      - /run:size=16M

    pids_limit: 128

    mem_limit: 256m

    cpus: 1.0