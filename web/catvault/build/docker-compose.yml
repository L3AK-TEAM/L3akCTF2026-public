services:
  catvault:
    build: .
    restart: unless-stopped
    container_name: catvault
    ports:
      - "5098:8080"
    volumes:
      - ./flag.txt:/flag.txt
    environment:
      MARIADB_RANDOM_ROOT_PASSWORD: "1"
      MARIADB_DATABASE: "catvault"
      MARIADB_USER: "meower"
      MARIADB_PASSWORD: "meowmeowmeow"
      FLAG: "L3AK{it_was_a_very_easy_web_challenge_sorry_to_bore_you_all_with_the_dumb_pretext_now_go_solve_the_real_one}"