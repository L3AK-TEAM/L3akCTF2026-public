# Is RestrictedPython Actually Restricted?
## Author: 0x1622

as the title says.
flag at: `/flag.txt`.

Submit one base64-encoded UTF-8 Python program per connection, followed by
`EOL` on its own line. Base64 padding is optional. Standard output is returned
to you.