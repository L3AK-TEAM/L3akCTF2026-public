# Isaac's Kaleidoscope: Fractal Block Server

This is the private build folder for the challenge service.

Run locally:

```sh
PORT=8767 node server.cjs
```

Run with Docker:

```sh
./run-docker.sh
```

The private flag is in `flag.txt`. Do not include this folder in the player
handout.
